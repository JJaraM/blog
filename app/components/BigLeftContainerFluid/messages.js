/*
 * BigLeftContainerFluid Messages
 *
 * This contains all the text for the BigLeftContainerFluid component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BigLeftContainerFluid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BigLeftContainerFluid component!',
  },
});
