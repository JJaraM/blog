/*
 * ButtonViewMore Messages
 *
 * This contains all the text for the ButtonViewMore component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ButtonViewMore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ButtonViewMore component!',
  },
});
