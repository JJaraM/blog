/*
 * IconClose Messages
 *
 * This contains all the text for the IconClose component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IconClose';

export default defineMessages({
  label: { id: `${scope}.label` },
});
