/*
 * IconDelete Messages
 *
 * This contains all the text for the IconDelete component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IconDelete';

export default defineMessages({
  label: { id: `${scope}.label` },
});
