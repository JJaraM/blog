/*
 * TestimonialList Messages
 *
 * This contains all the text for the TestimonialList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TestimonialList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialList component!',
  },
});
