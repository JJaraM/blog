/*
 * LatestPostItemList Messages
 *
 * This contains all the text for the LatestPostItemList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LatestPostItemList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LatestPostItemList component!',
  },
});
