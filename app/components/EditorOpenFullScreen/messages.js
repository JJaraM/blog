/*
 * EditorOpenFullScreen Messages
 *
 * This contains all the text for the EditorOpenFullScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditorOpenFullScreen';

export default defineMessages({
  label: {
    id: `${scope}.label`
  },
});
