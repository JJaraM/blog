/*
 * PostEditableContent Messages
 *
 * This contains all the text for the PostEditableContent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PostEditableContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PostEditableContent component!',
  },
});
