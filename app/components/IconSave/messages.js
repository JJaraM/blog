/*
 * IconSave Messages
 *
 * This contains all the text for the IconSave component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IconSave';

export default defineMessages({
  label: { id: `${scope}.label` },
  inProgress: { id: `${scope}.inProgress` },
  done: { id: `${scope}.done` },
  error: { id: `${scope}.error` },
});
