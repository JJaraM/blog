/*
 * EditorCloseFullScreen Messages
 *
 * This contains all the text for the EditorCloseFullScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditorCloseFullScreen';

export default defineMessages({
  label: {
    id: `${scope}.label`
  },
});
