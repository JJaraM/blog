/*
 * EditableMetadata Messages
 *
 * This contains all the text for the EditableMetadata component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditableMetadata';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditableMetadata component!',
  },
});
