/*
 * DeletePost Messages
 *
 * This contains all the text for the DeletePost container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DeletePost';

export default defineMessages({
  header: { id: `${scope}.header` },
  description: {id: `${scope}.description`},
  delete: {id: `${scope}.delete`}
});
