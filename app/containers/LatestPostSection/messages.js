import { defineMessages } from 'react-intl';

export const scope = 'jjara.containers.LatestPostSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Latest Posts',
  },
});
