import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TestimonialSection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Testimonials',
  },
  subTitle : {
    id: `${scope}.subTitle`,
    defaultMessage: 'What`s the rumor about me',
  }
});
