// import { selectTestimonialSectionDomain } from '../selectors';

describe('selectTestimonialSectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
