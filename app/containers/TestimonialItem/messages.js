/*
 * TestimonialItem Messages
 *
 * This contains all the text for the TestimonialItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TestimonialItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TestimonialItem container!',
  },
});
