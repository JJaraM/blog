/*
 * PostPageCreate Messages
 *
 * This contains all the text for the PostPageCreate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PostPageCreate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PostPageCreate container!',
  },
});
