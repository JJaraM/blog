// import { selectPostPageCreateDomain } from '../selectors';

describe('selectPostPageCreateDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
