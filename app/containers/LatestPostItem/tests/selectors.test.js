// import { selectLatestPostItemDomain } from '../selectors';

describe('selectLatestPostItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
