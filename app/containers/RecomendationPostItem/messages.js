/*
 * LatestPostItem Messages
 *
 * This contains all the text for the LatestPostItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LatestPostItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LatestPostItem container!',
  },
});
